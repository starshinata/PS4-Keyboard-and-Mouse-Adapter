<Window x:Class="PS4KeyboardAndMouseAdapter.MouseSettings"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PS4KeyboardAndMouseAdapter"
        xmlns:controls="clr-namespace:PS4KeyboardAndMouseAdapter.UI.Controls"
        mc:Ignorable="d"
        Title="Mouse Settings" Height="220" Width="380">

    <Window.DataContext>
        <local:MouseSettingsViewModel/>
    </Window.DataContext>
    
    <Window.Style>
        <Style TargetType="Window" BasedOn="{StaticResource {x:Type Window}}"/>
    </Window.Style>

    <TextBlock Text="BBB" FontSize="42"></TextBlock>
    <!-- -->
    <!-- <controls:MouseSettingsControl MouseSensitivity="{Binding MouseSensitivity, Mode=TwoWay}" -->
    <!--                                HorizontalVerticalRatio="{Binding HorizontalVerticalRatio, Mode=TwoWay}" -->
    <!--                                Deadzone="{Binding Deadzone, Mode=TwoWay}" -->
    <!--                                Margin="25, 20, 25, 20"/> -->

</Window>
