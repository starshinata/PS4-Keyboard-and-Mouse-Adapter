<UserControl x:Class="PS4KeyboardAndMouseAdapter.UI.Controls.MiscellaneousSettingsController"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             
             d:DesignHeight="700" 
             d:DesignWidth="700"
             mc:Ignorable="d" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             >

    <Grid >
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <StackPanel
            Grid.Column="0">
            <Button 
                Content="Reset RemotePlay window location"
                Click="Handler_ResetRemotePlay"
            />

            <!-- RichTextBox for word wrap -->
            <RichTextBox  
                Grid.Column="0"
                Grid.Row="2"
                >
                <FlowDocument>
                    <Paragraph>
                        Sometimes when you have enabled mouse input for RemotePlay, then sometimes you can drag the RemotePlay window offscreen
                    </Paragraph>
                </FlowDocument>
            </RichTextBox>

        </StackPanel>
    </Grid>
</UserControl>
